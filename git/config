[core]
	editor = nvim
[alias]
	co = checkout
	ds = diff --staged
	st = status
	lg = log --all --graph --oneline
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
